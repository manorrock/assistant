{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Tool Result Schema",
  "description": "Schema for results returned by tool executions",
  "type": "object",
  "required": ["success", "message"],
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the tool execution was successful"
    },
    "message": {
      "type": "string",
      "description": "Human-readable message describing the result or error"
    },
    "data": {
      "description": "Result data from the tool execution, only present on success",
      "oneOf": [
        {"type": "string"},
        {"type": "number"},
        {"type": "boolean"},
        {"type": "object"},
        {"type": "array"}
      ]
    }
  }
}
