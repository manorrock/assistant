# Task: 0001 - Add /llmEndpoint to NetBeans plugin

## Original Task Description
Add /llmEndpoint to NetBeans plugin

## Implementation Scope

### Affected Components
- [x] Maven multi-module project
  - Specific modules: netbeans
- [ ] IntelliJ plugin (gradle)
- [ ] Eclipse plugin (eclipse)
- [ ] CLI implementation (cli)
- [ ] VSCode extension (vscode)

### File Changes Required

#### NetBeans Plugin
1. `netbeans/src/main/java/com/manorrock/assistant/netbeans/NetBeansControllerTopComponent.java`
   - Add /llmEndpoint command handling
   - Add method to update LLM endpoint URL

### Cross-Implementation Requirements
- Consistent command handling across implementations
- Error handling for invalid URLs
- UI feedback for endpoint updates
- Command documentation updates

### Documentation Updates
- [ ] Update README.md command matrix to include /llmEndpoint for NetBeans
- [ ] Update NetBeans plugin documentation with new command

### Testing Requirements
- [ ] Unit tests for /llmEndpoint command
- [ ] Test invalid URL handling
- [ ] Test endpoint update functionality

## Task Completion Checklist
1. [ ] Implementation for NetBeans plugin
2. [ ] Documentation updates
3. [ ] Test coverage
4. [ ] Command matrix update
5. [ ] Remove task from:
   - [ ] plan.md
   - [ ] ROADMAP.md

## Implementation Plan
1. Update NetBeansControllerTopComponent.java:
   - Add command handling for /llmEndpoint
   - Add method for URL validation
   - Add method to update endpoint URL
   - Add UI feedback for successful/failed updates

2. Add unit tests:
   - Test successful endpoint update
   - Test invalid URL handling
   - Test command recognition

3. Update documentation:
   - Add command to README.md matrix
   - Document command usage in NetBeans docs

## LLM Implementation Prompt

```
You are tasked with implementing the /llmEndpoint command in the NetBeans plugin for Manorrock Assistant. Please make the following changes:

1. Update the NetBeansControllerTopComponent class in `netbeans/src/main/java/com/manorrock/assistant/netbeans/NetBeansControllerTopComponent.java`:
   - Add handling for /llmEndpoint command to update the ollamaEndpoint variable
   - Add URL validation
   - Add UI feedback for successful/failed updates
   - Ensure changes follow existing codebase patterns
   - Maintain consistent error handling

2. Update documentation:
   - Update command matrix in README.md
   - Update plugin documentation

3. After implementation:
   - Remove task from plan.md
   - Remove task from ROADMAP.md
   - Remove any completed tasks
   - If no tasks remain in plan.md, remove it

Review the current implementation in `netbeans/src/main/java/com/manorrock/assistant/netbeans/NetBeansControllerTopComponent.java` and ensure changes integrate smoothly with existing code.

Important considerations:
1. Maintain consistent command handling
2. Follow existing error handling patterns
3. Provide clear user feedback
4. Validate URLs before updating
5. Update only required code sections
```