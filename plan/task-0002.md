# Task: 0002 - Add /new command to CLI

## Original Task Description
Add /new command to CLI

## Implementation Scope

### Affected Components
- [x] Maven multi-module project
  - Specific modules: cli, shared
- [ ] IntelliJ plugin (gradle)
- [ ] Eclipse plugin (eclipse)
- [ ] CLI implementation (cli)
- [ ] VSCode extension (vscode)

### Existing Implementation
The `/new` command already has a basic implementation in:
- `shared/src/main/java/com/manorrock/assistant/shared/NewCommand.java`
- Command is registered in `cli/src/main/java/com/manorrock/assistant/cli/CLI.java`

### File Changes Required

#### Shared Module
1. `shared/src/main/java/com/manorrock/assistant/shared/NewCommand.java`
   - Already implemented
   - Add session ID reset support
   - Add chat history export before clearing

#### CLI Module
1. `cli/src/main/java/com/manorrock/assistant/cli/CLI.java`
   - Already implemented
   - Update startNewSession() to handle session ID reset
   - Add history export functionality

#### Documentation
1. `README.md`
   - Update command support matrix (already done)

### Cross-Implementation Requirements
- Consistent command handling across implementations
- Session ID management
- History preservation options
- State management synchronization

### Testing Requirements
- [x] Basic command execution
- [x] Session state verification
- [x] History clearing validation
- [x] File at `cli/src/test/java/com/manorrock/assistant/cli/NewCommandTest.java` already exists and tests basic functionality

### Dependencies
- Command registry system
- State persistence mechanism
- Session management

### Implementation Plan

1. Update NewCommand Implementation
   - Verify session ID reset
   - Add history export 
   - Update command description

2. Update CLI Integration
   - Verify session ID handling
   - Add history backup
   - Ensure state persistence

3. Testing
   - Review existing test coverage
   - Add edge case tests if needed

4. Documentation
   - Verify README.md is up to date
   - Remove task from plan.md
   - Remove task from ROADMAP.md

### Success Criteria
1. `/new` command successfully creates new chat session
2. Session ID is properly reset
3. History is correctly managed
4. State is properly persisted
5. All tests pass
6. Documentation is updated

### LLM Implementation Prompt

```
You are working on the Manorrock Assistant project, specifically enhancing the /new command implementation. The basic implementation exists but needs refinement. Make the following changes:

1. Update shared/src/main/java/com/manorrock/assistant/shared/NewCommand.java:
   - Add session ID reset support
   - Add history export functionality
   - Update command description to reflect new capabilities
   - Keep existing implementation structure
   - Maintain error handling patterns

2. Update cli/src/main/java/com/manorrock/assistant/cli/CLI.java:
   - Add history backup before clearing in startNewSession()
   - Reset session ID in startNewSession()
   - Ensure state persistence after session reset
   - Keep existing command registration

3. Update README.md:
   - Verify command support matrix is accurate
   - No changes needed if already correct

4. Remove task from:
   - Remove task from plan.md
   - Remove task from ROADMAP.md
   - Update completed task status
   - If no tasks remain in plan.md, remove it

Existing files for reference:
- `shared/src/main/java/com/manorrock/assistant/shared/NewCommand.java`
- `cli/src/main/java/com/manorrock/assistant/cli/CLI.java`
- `cli/src/test/java/com/manorrock/assistant/cli/NewCommandTest.java`

Implementation notes:
1. Follow existing code patterns
2. Maintain consistent error handling
3. Keep state management consistent
4. Preserve test coverage
5. Use existing configuration mechanisms

